export type categoryType = {
    name: string;
    color: string;
    expense: boolean
}

export type infoType = {
    date: Date,
    category: string,
    title: string, 
    value: number,
    expense: boolean
}